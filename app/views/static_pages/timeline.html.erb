<div class="row">
    <%= render "side" %>
    <div class="col-md-8">
            <h2>タイムライン</h2>
            <% if @feed_items.present? %>
            <ol class="posts">
            <%= render 'shared/feed' %>
            </ol>
            <% else %>
            記事はありません。
            <% end %>
    </div>
    <aside class="col-md-2">
    </aside>
</div>